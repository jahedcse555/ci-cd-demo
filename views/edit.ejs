<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Edit News</title>
<link rel="stylesheet" href="/style.css">
</head>
<body>
<div class="container">
  <h2>Edit News Article</h2>
  <form method="POST" action="/edit/<%= article.id %>" enctype="multipart/form-data">
    <label>Your Name</label>
    <input type="text" name="author" value="<%= article.author %>" required>

    <label>Title</label>
    <input type="text" name="title" value="<%= article.title %>" required>

    <label>Content</label>
    <textarea name="content" rows="6" required><%= article.content %></textarea>

    <% if(article.image) { %>
      <p>Current Image:</p>
      <img src="<%= article.image %>" alt="Current Image" style="width:100%;max-height:250px;object-fit:cover;margin-bottom:1rem;">
    <% } %>

    <label>Change Image</label>
    <input type="file" name="image" accept="image/*">

    <button type="submit" class="btn btn-primary">Update</button>
  </form>
  <a href="/" class="back-link">← Back to Home</a>
</div>
</body>
</html>

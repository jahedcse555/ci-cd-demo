<!DOCTYPE html>
<html>
<head>
  <title><%= article.title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <h1><%= article.title %></h1>
  </header>

  <main>
    <% if (article.image) { %>
      <img src="<%= article.image %>" alt="News Image" class="detail-image">
    <% } %>
    <p><%= article.content %></p>
    <p><small>By <%= article.author %> | <%= article.createdAt.toLocaleString() %></small></p>
    <a href="/">⬅ Back to Home</a>
  </main>

  <footer>
    <p>&copy; 2025 News Portal</p>
  </footer>
</body>
</html>

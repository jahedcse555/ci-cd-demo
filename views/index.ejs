<% if (news.length === 0) { %>
  <p>No news articles yet.</p>
<% } else { %>
  <% news.forEach(article => { %>
    <article>
      <h2><%= article.title %></h2>
      <p><%= article.content %></p>
      <small>🕒 <%= article.created_at %></small>
    </article>
  <% }) %>
<% } %>

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>NewsPortal</title>
<link rel="stylesheet" href="/style.css">
</head>
<body>

<header class="site-header">
  <div class="container header-container">
    <h1 class="site-logo"><a href="/">📰 Jahed's News Portal</a></h1>
    <nav>
      <a href="/new" class="btn btn-primary">Write News</a>
    </nav>
  </div>
</header>

<main class="container">
  <% if(news.length === 0){ %>
    <p class="empty-msg">No news yet. Be the first to publish!</p>
  <% } else { %>
    <section class="news-grid">
      <% news.forEach(article => { %>
        <div class="news-card">
          <% if(article.image){ %>
            <img src="<%= article.image %>" alt="<%= article.title %>">
          <% } %>
          <h3 class="news-title"><%= article.title %></h3>
          <p class="news-summary"><%= article.summary %></p>
          <a href="/news/<%= article.id %>" class="read-more-btn">Read More</a>
        </div>
      <% }) %>
    </section>
  <% } %>
</main>

<footer class="site-footer">
  <div class="container">
    <p>&copy; 2025 NewsPortal. All rights reserved.</p>
  </div>
</footer>

</body>
</html>
